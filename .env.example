# ====================
# AI PROVIDER API KEY
# ====================
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxx

# ====================
# CLERK AUTHENTICATION (Optional - for token validation)
# ====================
# Get from: https://dashboard.clerk.com
# Only needed if you want the backend to validate Clerk tokens
# For production deployment of the web app
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxx

# ====================
# SUPABASE DATABASE (Optional - for usage tracking)
# ====================
# Get from: https://supabase.com/dashboard
# Only needed if you want to track user usage
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ0eXAi...

# ====================
# SERVER CONFIG
# ====================
PORT=3000
NODE_ENV=development
```

Get your API key from: https://platform.openai.com/api-keys

### 3. Start the Server
```bash
npm start
```

The server will run on `http://localhost:3000`

## API Endpoints

### GET /health
Check if the backend is running.
```bash
curl http://localhost:3000/health
```

### POST /generate
Generate an AI reply for a post.

**Request:**
```bash
curl -X POST http://localhost:3000/generate \
  -H "Content-Type: application/json" \
  -d '{"text": "Just launched our new product!"}'
```

**Response:**
```
That's awesome! Congratulations on the launch. Would love to hear more about it!
```

## Environment Variables

- `OPENAI_API_KEY` - Your OpenAI API key (required)
- `PORT` - Server port (default: 3000)

## Development

For development with auto-restart:
```bash
npm run dev
```

(Requires nodemon to be installed)
